import React from "react";


function Test(props) {
    return(<div className={"Test"}>
        <h1>Sample Data</h1>

        {/*{props.testData.sample.map((sample_data, key) => {
            return <p>{sample_data}</p>
        })}*/}
    </div>)
}

export default Test;